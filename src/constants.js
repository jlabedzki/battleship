const userGrid = $('.userGrid');
const computerGrid = $('.computerGrid');
const shipGrid = $('.shipGrid');
const ships = $('.ship');
const destroyer = $('.destroyerContainer');
const submarine = $('.submarineContainer');
const cruiser = $('.cruiserContainer');
const battleship = $('.battleshipContainer');
const carrier = $('.carrierContainer');
const startGame = $('#start');
const rotateShips = $('rotateShips');
const turn = $('#whoseTurn');
const messageLog = $('#messageLog');


module.exports = { userGrid, computerGrid, shipGrid, ships, destroyer, submarine, cruiser, battleship, carrier, startGame, rotateShips, turn, messageLog };